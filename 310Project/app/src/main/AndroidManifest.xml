<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.appfactory.kaldi">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".StoreProfileActivity"
            android:label="@string/title_store_profile"></activity>
        <activity
            android:name=".AddMenuItemActivity"
            android:label="@string/title_add_menu_item"/>
        <activity
            android:name=".AddStoreActivity"
            android:label="@string/title_add_store"/>
        <activity
            android:name=".AddBusinessActivity"
            android:label="@string/title_add_store"/>
        <activity
            android:name=".CheckoutActivity"
            android:label="@string/title_check_out" />
        <activity
            android:name=".MenuActivity"
            android:label="@string/title_menu" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".DrinkerMainActivity" />
        <activity
            android:name=".ManageStoreActivity"
            android:label="@string/title_stores" />
        <activity
            android:name=".MerchantMainActivity"
            android:label="@string/title_merchant" />
        <activity
            android:name=".ManageProfileActivity"
            android:label="@string/title_manage_profile" />
        <activity
            android:name=".OrderHistoryActivity"
            android:label="@string/title_order_history" />
        <activity
            android:name=".RegisterMerchantActivity"
            android:label="@string/title_registration" />
        <activity
            android:name=".RegisterDrinkerActivity"
            android:label="@string/title_registration" />
        <activity
            android:name=".Register"
            android:label="@string/title_select_registration" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>